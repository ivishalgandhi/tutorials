# Tutorials

Self-paced technical courses — HTML lessons with quizzes, Mermaid diagrams, and learning records.

| Course | Description | Lessons |
|---|---|---|
| [postgres-scanner](./postgres-scanner/) | PostgreSQL internals for Go scanner builders | 6 |

## Browsing lessons

Lessons use relative asset paths and must be served over HTTP — not opened as `file://` URLs.

```bash
# from repo root
python3 -m http.server 8765 --directory postgres-scanner
# then open http://localhost:8765/lessons/
```
